# Rally Audio Pipeline — Python dependencies
# Install with: pip install -r requirements.txt

# Audio processing
numpy==1.26.4
scipy
soundfile
matplotlib

# Speech separation
deepfilternet

# Transcription
openai-whisper

# Optional: better fallback separation (used if DeepFilterNet fails)
librosa